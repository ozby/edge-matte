---
type: blueprint
status: draft
complexity: M
created: '2026-07-11'
last_updated: '2026-07-11'
progress: '0% (drafted)'
depends_on: []
tags:
  - ci
  - agent-kit
  - supply-chain
---

# Migrate agent-kit CI bootstrap to immutable setup-wp

**Goal:** Use the released `wp setup` migration to move every edge-matte-owned
CI installation of `wp` from duplicated inline package pins to one immutable,
self-versioning upstream `setup-wp` action.

## Architecture references

- [Architecture narrative](../../../docs/architecture.md)
- [Architecture contract](../../../docs/architecture.contract.json)
- Deployment target remains `edge-matte.ozby.dev`.

This is CI toolchain maintenance. It does not change runtime topology,
deployment shape, storage boundaries, or the public application contract.

## Key decisions

| Decision | Choice | Rationale |
| --- | --- | --- |
| Version source | The immutable setup action's checked-in package metadata | The action ref and installed package version advance together, eliminating consumer-side version plumbing. |
| Migration owner | Released `wp setup` | Consumer workflows should be generated/migrated by agent-kit rather than patched independently in every repository. |
| Installer | Immutable `webpresso/agent-kit/.github/actions/setup-wp@<40-character commit>` with no version input | The upstream action installs its own released version globally through Vite+ and is supply-chain pinned. |
| Consumer dependency | Keep agent-kit global-only | Preserves the thin-consumer contract and avoids adding package or lockfile ownership. |
| Regression coverage | Discover every workflow YAML file at test runtime | Newly added workflows are covered automatically and cannot hide legacy direct installs. |

## Execution plan

#### [qa] Task 1.1: Lock the workflow bootstrap contract

**Status:** todo

**Depends:** None

Validate the discovery-based contract test generated by `wp setup` across all
`.yml` and `.yaml` files under `.github/workflows`. The test must reject inline
`@webpresso/agent-kit` installation specs, legacy version variables, mutable or
repo-local setup actions, and any consumer-owned agent-kit version input. It
must prove every direct `wp` installation uses one immutable, self-versioning
upstream action.

**Files:**

- Create or modify through `wp setup`: `test/agent-kit-workflow-bootstrap.test.ts`

**Acceptance:**

- [ ] The test discovers both workflow YAML extensions recursively.
- [ ] The test fails against the pre-migration direct 2.4.1 installs.
- [ ] No consumer workflow or config owns a duplicate installed package version.

#### [infra] Task 1.2: Run the upstream setup migration

**Status:** todo

**Depends:** Task 1.1

Run `wp setup` from the released agent-kit version that owns the self-versioning
setup action migration. Review and commit only its generated workflow migration
plus necessary edge-matte-specific 3.x compatibility changes. If setup does not
migrate every direct install, report the missing case upstream rather than
hand-authoring a consumer-only workaround. Do not add a local composite action,
a repository dependency, a resolver step, or a second version default.

**Files:**

- Modify through `wp setup`: `.github/workflows/ci.yml`
- Modify through `wp setup`: `.github/workflows/architecture-drift.yml`
- Modify through `wp setup`: `.github/workflows/release.yml`

**Acceptance:**

- [ ] All five direct install jobs call the same immutable self-versioning action.
- [ ] No workflow contains `AGENT_KIT_VERSION=2.4.1` or a direct agent-kit package install.
- [ ] The existing Vite+ package source and job behavior remain unchanged.

#### [qa] Task 1.3: Validate agent-kit 3.1.10 ownership and repo gates

**Status:** todo

**Depends:** Task 1.2

Run the exact published `3.1.10` audit surface relevant to the changed harness
contract. Add `runtime-runtime-state` only if the exact audit proves it is
required and derive its shape from current agent-kit ownership rules. Preserve
historical blueprint failures as explicit validation gaps rather than weakening
or rewriting unrelated blueprint history.

**Files:**

- Modify if required: `catalog/agent/harness-surfaces.yaml`

**Acceptance:**

- [ ] Exact 3.1.10 harness and guardrail audits pass for the current manifest.
- [ ] Targeted workflow contracts, typecheck, lint, tests, and format checks pass.
- [ ] Any unrelated historical blueprint-lifecycle failure is reported without being papered over.

## Verification gates

| Gate | Command | Success criteria |
| --- | --- | --- |
| Workflow contract | `vp run test -- test/agent-kit-workflow-bootstrap.test.ts` | Discovery contract passes |
| Exact harness audit | `npx -y -p @webpresso/agent-kit@3.1.10 wp audit harness-surfaces` | Manifest passes under 3.1.10 |
| Exact guardrails | `npx -y -p @webpresso/agent-kit@3.1.10 wp audit guardrails` | Guardrails pass under 3.1.10 |
| Type safety | `wp typecheck` | Zero errors |
| Lint | `wp lint` | Zero violations |
| Tests | `wp test --file vitest.config.ts` | All repo tests pass |
| Format | `wp format --check` | No formatting drift |
| Blueprint history | `npx -y -p @webpresso/agent-kit@3.1.10 wp audit blueprint-lifecycle` | Pass, or pre-existing historical failures recorded verbatim |

## Non-goals

- Adding `@webpresso/agent-kit` to any `package.json` or lockfile.
- Copying the upstream setup action into edge-matte.
- Changing application runtime architecture or deployment behavior.
- Rewriting historical blueprints solely to make a new audit version green.

## Risks

| Risk | Impact | Mitigation |
| --- | --- | --- |
| A future workflow bypasses the self-versioning action | CI jobs drift across agent-kit releases | Recursive discovery test rejects direct installs, version inputs, and legacy pins in every workflow YAML file. |
| The action runs before Vite+ is available | `setup-wp` exits because `vp` is missing | Preserve each job's existing exact Vite+ bootstrap before invoking the action. |
| Agent-kit 3.x adds harness ownership requirements | Harness gate fails after migration | Run exact 3.1.10 audits and add only evidence-backed manifest ownership. |
