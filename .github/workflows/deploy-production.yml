name: Deploy production

on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      release_version:
        description: "Semver release version already committed in infra/release-metadata.production.json"
        required: true
        type: string

concurrency:
  group: edge-matte-production-deploy
  cancel-in-progress: false

jobs:
  resolve-release:
    runs-on: ubuntu-latest
    outputs:
      release_version: ${{ steps.resolve.outputs.release_version }}
    steps:
      - name: Resolve release version
        id: resolve
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          REF_NAME: ${{ github.ref_name }}
          INPUT_RELEASE_VERSION: ${{ inputs.release_version }}
        run: |
          set -euo pipefail
          if [[ "${EVENT_NAME}" == "push" ]]; then
            release_version="${REF_NAME#v}"
          else
            release_version="${INPUT_RELEASE_VERSION}"
          fi
          if [[ ! "${release_version}" =~ ^[0-9]+\.[0-9]+\.[0-9]+([-+][0-9A-Za-z.-]+)?$ ]]; then
            echo "Invalid semantic release version: ${release_version}" >&2
            exit 1
          fi
          printf 'release_version=%s\n' "${release_version}" >> "$GITHUB_OUTPUT"

  deploy:
    needs: resolve-release
    uses: webpresso/agent-kit/.github/workflows/cloudflare-production.yml@1db9b8d3de11a67096b9cc56ed988ba95d6cc221
    with:
      install_command: pnpm install --frozen-lockfile
      verify_command: |
        pnpm exec vp run verify:secrets
        pnpm exec wp audit absolute-path-policy --root .
        pnpm exec wp audit no-first-party-mjs --root .
        pnpm exec vp run audit:secret-provider-quarantine
        pnpm exec vp run audit:blueprint-links
        pnpm exec vp run format:check
        pnpm exec vp run check-types
        pnpm exec vp run lint
        pnpm exec wp audit docs-frontmatter
        pnpm exec wp audit blueprint-lifecycle
        pnpm exec wp audit architecture-drift --root .
        pnpm exec vp run verify:deploy-contract
        pnpm exec vp run build
        pnpm exec vp run test
        pnpm exec vp exec --filter @edge-matte/e2e -- playwright install --with-deps chromium
        pnpm exec vp run e2e -- --suite upload-delete-contract
        pnpm exec vp run e2e -- --suite smoke
        pnpm exec vp run e2e -- --suite upload-delete
        bash scripts/verify-cloudflare-deploy-creds.sh
        if [[ -n "${CF_ACCESS_CLIENT_ID:-}" || -n "${CF_ACCESS_CLIENT_SECRET:-}" ]]; then [[ -n "${CF_ACCESS_CLIENT_ID:-}" && -n "${CF_ACCESS_CLIENT_SECRET:-}" ]]; fi
      deploy_command: pnpm exec wp deploy --lane prd
      smoke_command: |
        bash scripts/wait-for-http.sh "https://edge-matte.ozby.dev/health" 24 5
        bash scripts/wait-for-http.sh "https://edge-matte.ozby.dev/" 12 5
        E2E_RUN_PRODUCTION=1 pnpm exec vp run e2e -- --suite production-smoke
        E2E_RUN_PRODUCTION=1 pnpm exec vp run e2e -- --suite production-journey
      release_version: ${{ needs.resolve-release.outputs.release_version }}
      runner: ubuntu-latest
    secrets:
      ci_secret_provider_token: ${{ secrets.DOPPLER_SERVICE_TOKEN || secrets.DOPPLER_TOKEN }}
