name: Security scan

on:
  push:
    branches: [main, master]
  pull_request:
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:

jobs:
  security:
    if: ${{ github.event_name != 'pull_request' || github.event.pull_request.head.ref != 'changeset-release/main' }}
    uses: webpresso/github-actions/.github/workflows/webpresso-security.yml@aaf5e03cb7b215bcc8232858ebcf4cbbec2a8c0e
    with:
      install_command: vp install --frozen-lockfile
      security_command: vp exec secretlint "**/*"
      runner: ubuntu-latest
