name: Security scan

on:
  push:
    branches: [main, master]
  pull_request:
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:

jobs:
  security:
    if: ${{ github.event_name != 'pull_request' || github.event.pull_request.head.ref != 'changeset-release/main' }}
    uses: webpresso/github-actions/.github/workflows/webpresso-security.yml@8ef11734f666ea86f41a129e634ba68e91638df6
    with:
      install_command: vp install --frozen-lockfile
      security_command: vp exec secretlint "**/*"
      runner: ubuntu-latest
