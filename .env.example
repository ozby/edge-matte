# EdgeMatte — non-secret onboarding placeholders only.
# Do not copy this file to .env or .env.local.
# Provider and deploy secrets live in your secret manager (e.g. Doppler) and Cloudflare.

# Public app origin (documented default; production value is set in apps/workers/wrangler.toml)
PUBLIC_BASE_URL=https://edge-matte.ozby.dev
